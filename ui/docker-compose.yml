version: '3.7'

services:
  ui:
    container_name: ui
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - '.:/ui'
      - '/ui/node_modules'
    ports:
      - 3001:3000
    environment:
      - CHOKIDAR_USEPOLLING=true
